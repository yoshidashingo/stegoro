# ステアリングポリシー生成ルール

このルールは `skills/generate/**/*.md` 配下を編集する際に適用される（ポリシー生成スキル開発時）。
## ステアリングポリシー生成ルール

このルールはステアリングポリシーの生成作業に適用される。

### エントリポイント

`skills/generate/SKILL.md` を最初に読み込むこと。

### ルール詳細の読み込み

各フェーズの作業時に `skills/generate/rule-details/` 配下の該当ファイルを読み込む:

- **common/**: 全フェーズ共通のルール（最初に読み込む）
- **discovery/**: 目的分析・ドメインリサーチフェーズ
- **design/**: ワークフロー設計・ルール設計フェーズ
- **generation/**: ルールファイル生成フェーズ
- **refinement/**: 品質レビュー・キャリブレーションフェーズ

### 適用条件

以下の依頼時にこのルールセットを使用する:

- `/stegoro:generate` スキルの実行
- 「XXXのステアリングポリシーを生成して」
- 「XXX業務のエージェントを作って」
- 目的別エージェント用ポリシーの作成全般
