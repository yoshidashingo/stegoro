# Markdown ドキュメント規約

このルールは `**/*.md` ファイル編集時に適用される。
## ドキュメント規約

- 1ファイル1トピック — 簡潔で焦点を絞った内容にする
- ドキュメント間は相対リンクを使用（絶対GitHub URLは避ける）
- 見出しの階層を飛ばさない（例: `##` の次は `###`、`####` に飛ばさない）
- 変更時はリンク切れがないか確認する
